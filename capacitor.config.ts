import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brivaltop.imagecompressor.resizer.mbtokb',
  appName: 'Image Compressor & Resizer in KB',
  webDir: 'dist',
};

export default config;
